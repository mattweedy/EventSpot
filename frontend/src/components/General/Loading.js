import React from 'react';

const Loading = () => {
    return (
        <div>
            <h1 style={{ justifyContent: 'center', alignContent: 'center', marginTop: '150px' }}>Loading...</h1>
        </div>
    );
};

export default Loading;